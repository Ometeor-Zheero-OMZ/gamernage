version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm install
    build:
      commands:
        - cd frontend
        - npm run build
    postBuild:
      commands:
        - cd frontend
        - npm run export
  artifacts:
    baseDirectory: /out
    files:
      - "**/*"
  cache:
    paths:
      - frontend/node_modules/**/*
